# Spring properties
spring:
  application:
    name: board-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 1115   # HTTP (Tomcat) port